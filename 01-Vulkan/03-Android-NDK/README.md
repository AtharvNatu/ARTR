# DEPLOY INSTRUCTIONS

- Copy the `libVkLayer_khronos_validation.so` and paste into `\app\src\main\cpp\lib\arm64-v8a` for every app before deploying.

- This file was earlier present in every app sub-directory, but due to considerable size, it is now kept as a single common lib file.