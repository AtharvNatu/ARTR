-  To use Shaders as a string, we are going to use - 
    1) GLSLang Library
    2) SPIR-V Library
written using C++ for converting GLSL, HLSL and Metal Shading Language (MSL) / Metal Performance Shaders (MPS) to SPIR-V format.

- So, we are going to change the extension from .c to .cpp

- This code contains additional headers and import libraries which may be bound with Vulkan SDK 1.4.304. They may not work with earlier or future versions and changes may need to be made for it.
